# Club Documentation

Markdown documentation site built with Jekyll and the `just-the-docs` theme.

## Local Development

Install Ruby and Bundler, then run:

```sh
bundle install
bundle exec jekyll serve
```

Open `http://localhost:4000` to preview the site.

The home page is `index.md`. Documentation pages live in `docs`.
